export const APP_URL = 'http://localhost:3000/api/';

export const categoryUrl = {
    baseUrl: APP_URL + 'categories/'
};

export const productUrl = {
    baseUrl: APP_URL + 'products/'
};

export const promotionUrl = {
    baseUrl: APP_URL + 'promotions/'
};
